# 锅炉制造企业 AI 化办公实施方案

> 基于元景万悟智能体平台（Wanwu AI Agent Platform）
> 编制日期：2026-05-10

---

## 第一部分：AI 化办公整体实施方案

---

### 一、项目背景与目标

贵公司作为锅炉制造行业企业，计划基于元景万悟智能体平台实现 AI 化办公转型，后期需接入 ERP、MES、WPS（工作流）等系统，同时需要妥善处理财务、人事等敏感数据。本方案旨在提供从架构设计到落地实施的完整路径。

**核心目标**：
1. 建立安全可控的 AI 办公基础设施
2. 实现与 ERP/MES/WPS 等业务系统的深度集成
3. 确保敏感数据的全生命周期安全
4. 构建细粒度的权限管控体系
5. 优先落地"体系管理手册纠错"场景

---

### 二、部署架构方案

#### 2.1 私有部署 vs API 模型部署对比

| 维度 | 私有部署（全栈本地化） | API 模型部署（混合模式） |
|------|----------------------|------------------------|
| **数据安全** | ★★★★★ 所有数据不出内网，最高安全等级 | ★★★☆☆ 非敏感数据可走 API，敏感数据需本地处理 |
| **合规性** | 完全满足等保/行业监管要求 | 需评估数据出境/第三方合规风险 |
| **初始成本** | 高（GPU 服务器 + 存储 + 网络） | 低（按量付费，无需硬件投入） |
| **运维成本** | 高（需专业运维团队） | 低（云服务商负责基础设施） |
| **模型性能** | 取决于本地 GPU 算力（RTX 4090D 可运行 Qwen3.6-27B） | 可随时调用最强模型（GLM-5.1/Qwen3.6-Max） |
| **网络要求** | 内网即可运行 | 需稳定外网连接 |
| **扩展性** | 受硬件限制，扩展需采购 | 弹性扩展，按需使用 |
| **适用场景** | 涉密/高合规/核心业务 | 非敏感/试验性/低频业务 |

#### 2.2 推荐部署架构：混合模式

针对锅炉制造行业特点，推荐采用**私有部署为主 + API 模型为辅**的混合架构：

```
┌─────────────────────────────────────────────────────────────────┐
│                        企业内网（安全区）                          │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐     │
│  │              万悟平台私有化部署集群                        │     │
│  │                                                           │     │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐              │     │
│  │  │BFF-Service│  │Agent-Svc │  │Knowledge │              │     │
│  │  └──────────┘  └──────────┘  └──────────┘              │     │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐              │     │
│  │  │RAG-Service│  │IAM-Service│  │Model-Svc │              │     │
│  │  └──────────┘  └──────────┘  └──────────┘              │     │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐              │     │
│  │  │Workflow  │  │MCP-Svc   │  │Callback  │              │     │
│  │  └──────────┘  └──────────┘  └──────────┘              │     │
│  │                                                           │     │
│  │  ┌──────────────────────────────────────────────────┐   │     │
│  │  │        基础设施（MySQL/Redis/MinIO/Kafka/ES）      │   │     │
│  │  └──────────────────────────────────────────────────┘   │     │
│  │                                                           │     │
│  │  ┌──────────────────────────────────────────────────┐   │     │
│  │  │     本地模型推理服务（Ollama / vLLM）               │   │     │
│  │  │     Qwen3.6-27B / GLM-5-Turbo 等开源模型本地部署     │   │     │
│  │  └──────────────────────────────────────────────────┘   │     │
│  └─────────────────────────────────────────────────────────┘     │
│                              │                                    │
│                              │ 防火墙 / API 网关                   │
│                              ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐     │
│  │              外部 API 模型服务（非敏感场景）                │     │
│  │     联通元景 / OpenAI / 通义千问 / 百度千帆 等             │     │
│  └─────────────────────────────────────────────────────────┘     │
│                                                                   │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐     │
│  │   ERP 系统      │  │   MES 系统      │  │  WPS/工作流     │     │
│  │  (SAP/用友等)   │  │  (制造执行)     │  │  (审批/协同)    │     │
│  └───────┬────────┘  └───────┬────────┘  └───────┬────────┘     │
│          │                   │                   │               │
│          └───────────────────┼───────────────────┘               │
│                              │ MCP 协议 / REST API               │
└──────────────────────────────┼──────────────────────────────────┘
                               │
                        ┌──────┴──────┐
                        │  企业用户     │
                        └─────────────┘
```

#### 2.3 服务器资源规划（三阶段渐进式扩展）

> **核心原则**：先验证后投入、按需扩展、保护投资。每个阶段的硬件投入都可在下一阶段复用，避免重复采购。

##### 阶段一：验证期（1-3 个月，1-2 人使用）

**目标**：验证平台功能与纠错效果，不追求性能，以最低成本跑通全流程。

| 角色 | 配置 | 数量 | 参考价格 | 说明 |
|------|------|------|---------|------|
| **AI 推理服务器** | 1×RTX 4090D 24GB + 64GB RAM + 1TB SSD | 1 | 3-4 万 | Qwen3.6-27B Q4_K_M 量化推理 |
| **应用+数据服务器** | 16C/32GB RAM/1TB SSD | 1 | 1-1.5 万 | 万悟全栈 Docker Compose 部署 |

**验证期总投入**：约 **4-5.5 万**（含服务器整机）

**可运行的模型**：
- Qwen3.6-27B Q4_K_M（~17GB 显存，10-20 t/s）—— 纠错主力模型
- bge-m3 Embedding（~2GB 显存）—— RAG 向量化
- 单卡并发能力：约 2-4 个同时请求

##### 阶段二：生产期（3-12 个月，部门级使用）

**目标**：支撑体系管理部日常纠错业务，满足等保合规，支持多用户并发。

| 角色 | 配置 | 数量 | 参考价格 | 说明 |
|------|------|------|---------|------|
| **AI 推理服务器** | 2×RTX 4090D 24GB + 128GB RAM + 2TB SSD | 1 | 6-8 万 | 双卡并行推理或分别运行不同模型 |
| **应用服务器** | 32C/64GB RAM/500GB SSD | 1 | 2-3 万 | 万悟微服务集群 |
| **数据服务器** | 16C/32GB RAM/2TB SSD | 1 | 1.5-2 万 | MySQL/Redis/ES/MinIO |
| **消息服务器** | 8C/16GB RAM/500GB SSD | 1 | 0.8-1 万 | Kafka |

**生产期总投入**：约 **10-14 万**（新增硬件约 5-8 万，阶段一服务器可降级为数据服务器复用）

**可运行的模型**：
- 双卡并行：Qwen3.6-27B Q5_K_M（~21GB，更高质量）+ bge-m3
- 或分别部署：Qwen3.6-27B Q4_K_M + Qwen3.6-35B-A3B Q4_K_M（MoE 高速推理）
- 并发能力：约 8-15 个同时请求

##### 阶段三：扩展期（1 年后，全公司级使用）

**目标**：支撑全公司 AI 化办公，接入 ERP/MES/WPS，满足高并发与多场景需求。

| 角色 | 配置 | 数量 | 参考价格 | 说明 |
|------|------|------|---------|------|
| **AI 推理集群** | 4×RTX 4090D 24GB + 256GB RAM + 4TB SSD | 1-2 | 12-18 万/台 | 多模型并行、高并发推理 |
| **应用服务器** | 32C/64GB RAM/500GB SSD | 2-3 | 2-3 万/台 | 微服务集群高可用部署 |
| **数据服务器** | 32C/64GB RAM/4TB SSD | 2 | 2-3 万/台 | MySQL 主从 + ES 集群 + MinIO |
| **消息服务器** | 16C/32GB RAM/1TB SSD | 1 | 1-1.5 万 | Kafka 集群 |

**扩展期总投入**：约 **25-40 万**（新增硬件约 15-25 万，前期服务器全部复用）

**可运行的模型**：
- 4 卡服务器：可运行 Qwen3.6-27B Q8_0（近无损）或同时部署 2-3 个不同模型
- 双 4 卡服务器：可运行更大参数模型，或实现推理/Embedding 分离部署
- 并发能力：约 30-50 个同时请求

##### 渐进式扩展路径图

```
阶段一（验证期）              阶段二（生产期）              阶段三（扩展期）
┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│ 1×4090D 推理服务器 │     │ 2×4090D 推理服务器 │     │ 4×4090D 推理集群  │
│ （单卡，Q4量化）   │ ──→ │ （双卡，Q5量化）   │ ──→ │ （4卡，多模型并行）│
│                    │     │                    │     │                    │
│ 1×应用+数据服务器  │     │ 应用+数据+消息     │     │ 多台应用/数据服务器 │
│ （All-in-One）     │     │ （服务分离部署）   │     │ （高可用集群）     │
│                    │     │                    │     │                    │
│ 投入：4-5.5 万     │     │ 投入：10-14 万     │     │ 投入：25-40 万     │
│ 并发：2-4 请求     │     │ 并发：8-15 请求    │     │ 并发：30-50 请求   │
└──────────────────┘     └──────────────────┘     └──────────────────┘
         │                        │                        │
         │  前期硬件全部复用        │  前期硬件全部复用        │
         └────────────────────────┴────────────────────────┘
```

##### 信创适配方案（可选，应对国资委 79 号文）

国资委 79 号文要求 2027 年底前央企核心系统完成 100% 信创替代。对于 AI 推理服务器，信创适配方案如下：

| 组件 | 信创替代方案 | 价格对比 | 说明 |
|------|------------|---------|------|
| GPU | 华为昇腾 910B (64GB) | 6-7 万/卡（约 RTX 4090D 的 4 倍） | 国产 AI 芯片，性能接近 A100，推理场景性价比高 |
| GPU | 海光 DCU Z100L (32GB) | 2-3 万/卡 | 国产 GPU，兼容 CUDA 生态，迁移成本低 |
| CPU | 华为鲲鹏 920 / 海光 7000 系列 | 与 Intel 同级 | 国产 CPU，万悟平台已适配 |
| 操作系统 | 麒麟 V10 / 统信 UOS | 约 0.5 万/套 | 国产操作系统 |
| 数据库 | TiDB / OceanBase | 开源免费 | 万悟平台已支持 |

> **建议**：阶段一/二采用 RTX 4090D（性价比最优），阶段三根据信创政策要求逐步引入昇腾 910B 服务器。万悟平台已支持 MySQL/TiDB/OceanBase 数据库和华为鲲鹏 CPU，信创迁移路径清晰。

#### 2.4 国企数据安全合规要点

作为国有企业，AI 系统部署需满足以下合规要求：

##### 2.4.1 法律法规框架

| 法规/标准 | 核心要求 | 对本方案的影响 |
|----------|---------|-------------|
| 《网络安全法》（2026 修订版） | AI 系统须纳入网络安全等级保护，加强风险监测评估 | 平台须通过等保三级认证 |
| 《数据安全法》 | 数据分类分级保护，重要数据出境安全评估 | L3+ 数据禁止出境，必须本地处理 |
| 《个人信息保护法》 | 个人信息处理需告知同意，最小必要原则 | 人事数据需脱敏后才能进入 AI |
| 《网络数据安全管理条例》（2025） | 数据处理者承担主体责任，加密/备份/访问控制 | 全链路加密 + 审计日志 |
| 《生成式 AI 服务管理暂行办法》 | 算法备案、安全评估、内容标注 | 面向公众服务需备案，内部使用暂免 |
| 国资委 79 号文 | 2027 年底前核心系统 100% 信创替代 | 需规划信创迁移路径 |
| 等保 2.0 三级 | 身份鉴别/访问控制/安全审计/数据完整性/数据保密性/入侵防范 | 万悟平台已具备基础能力 |

##### 2.4.2 数据不出域保障措施

```
┌──────────────────────────────────────────────────────────────┐
│                    数据不出域保障体系                            │
│                                                                │
│  1. 物理隔离层                                                  │
│     ├── AI 推理服务器部署于企业内网安全区                         │
│     ├── 与外网之间部署防火墙 + API 网关                          │
│     └── L3+ 数据处理服务器禁止外网访问                           │
│                                                                │
│  2. 逻辑隔离层                                                  │
│     ├── API 网关白名单：仅允许已审核的外部 API 请求通过            │
│     ├── 数据分级路由：L3+ → 本地模型，L1-L2 → 可选 API          │
│     └── 敏感词拦截：BFF 层自动检测并拦截含敏感数据的出站请求       │
│                                                                │
│  3. 加密传输层                                                  │
│     ├── 内部服务间 gRPC mTLS 双向认证                           │
│     ├── 对外接口 HTTPS/TLS 1.3 加密                             │
│     └── 数据库字段级加密（财务/人事关键字段 AES-256）             │
│                                                                │
│  4. 审计追溯层                                                  │
│     ├── 全量操作日志（谁在何时访问了什么数据）                    │
│     ├── AI 对话记录留存（至少 180 天）                           │
│     ├── 数据访问异常告警                                        │
│     └── 定期安全审计报告                                        │
│                                                                │
│  5. API 安全层（混合模式下关键）                                 │
│     ├── API 调用前自动脱敏（姓名→张**，金额→***）                │
│     ├── API 供应商合规审查（数据存储地/处理方式/留存期限）         │
│     ├── API 调用日志全量记录                                    │
│     └── 优先选择国内合规云服务商（阿里云/华为云）                 │
└──────────────────────────────────────────────────────────────┘
```

##### 2.4.3 API 调用合规审查清单

在混合模式下使用 API 调用时，需对 API 供应商进行以下审查：

| 审查项 | 要求 | 阿里云百炼 | 华为云昇腾 | 智谱官方 | DeepSeek |
|--------|------|----------|----------|---------|----------|
| 数据存储地域 | 必须在中国内地 | ✅ 北京/上海 | ✅ 全国节点 | ✅ 北京 | ✅ 杭州 |
| 数据留存期限 | 推理完成后不保留用户数据 | ✅ 不留存 | ✅ 不留存 | ✅ 不留存 | ⚠️ 留存 30-180 天，备份可能更长 |
| 训练数据使用 | 禁止将 API 输入用于训练 | ✅ 可选退出 | ✅ 不用于训练 | ✅ 不用于训练 | ❌ 未明确排除，无 opt-out 机制 |
| 等保认证 | 三级及以上 | ✅ 等保四级 | ✅ 等保四级 | ✅ 等保三级 | ✅ 等保三级 |
| 数据出境 | 禁止出境 | ✅ 中国内地 | ✅ 中国内地 | ✅ 中国内地 | ✅ 中国内地 |
| 安全审计 | 支持操作日志导出 | ✅ | ✅ | ✅ | ⚠️ 有限支持 |
| 信创适配 | 支持国产化部署 | 部分 | ✅ 全栈 | 部分 | ⚠️ 仅 x86 GPU |
| 零留存选项 | 企业级零留存 API | ✅ 可协商 | ✅ 支持 | ⚠️ 有限 | ❌ 无公开零留存选项 |
| 已知安全事件 | 无重大数据泄露 | 无 | 无 | 无 | ❌ 2025.1 超 100 万条用户数据泄露 |
| Gartner 评估 | 推荐用于敏感场景 | ✅ | ✅ | ✅ | ❌ 不建议用于敏感数据 |

> **建议**：API 调用优先选择华为云昇腾（信创全栈适配，央企属性），其次阿里云百炼（模型丰富，5 折节省计划性价比高）。
>
> ⛔ **DeepSeek API 不推荐用于包含敏感数据的场景**（Gartner 2026 年 4 月明确不建议）。仅可用于完全公开、非敏感的辅助查询场景，且必须经过数据脱敏处理。详见 2.4.4 节。

##### 2.4.4 DeepSeek API 安全风险专项分析

> **核心结论**：使用外部 DeepSeek API + 本地数据库的组合方案，**仍然存在数据泄露风险**。本地数据库只能保证数据存储安全，无法阻止 RAG 检索后的上下文通过 API 流出。

**一、数据泄露路径分析**

```
用户提问
  │
  ▼
┌─────────────────────────────────────────────────────┐
│  本地 RAG 检索                                        │
│  ┌───────────┐    检索     ┌──────────────────┐      │
│  │ 本地数据库  │ ────────→ │ 检索到的上下文片段  │      │
│  │ (法规/标准/ │           │ (含敏感内容！)     │      │
│  │  财务/工艺) │           └────────┬─────────┘      │
│  └───────────┘                     │                │
│                                     ▼                │
│                           ┌──────────────────┐      │
│                           │ 组装完整 Prompt    │      │
│                           │ = 用户问题         │      │
│                           │ + RAG 上下文       │  ← 这里面包含了你的敏感数据！
│                           │ + 系统提示词        │      │
│                           └────────┬─────────┘      │
└────────────────────────────────────┼────────────────┘
                                     │
                          ┌──────────▼──────────┐
                          │  发送到 DeepSeek API  │  ← 数据离开你的网络！
                          │  (api.deepseek.com)  │
                          └──────────┬──────────┘
                                     │
                          ┌──────────▼──────────┐
                          │  DeepSeek 服务器处理   │  ← 你的数据被对方存储/处理
                          │  · 输入内容被记录      │
                          │  · 可能用于模型训练    │
                          │  · 备份留存 30-180 天  │
                          │  · 无零留存选项       │
                          └─────────────────────┘
```

**二、DeepSeek API 已知安全风险**

| 风险类别 | 具体问题 | 严重程度 |
|---------|---------|---------|
| **数据留存** | API 输入留存 30-180 天，备份可能更长；删除对话不保证备份同步删除 | 🔴 高 |
| **训练数据混入** | 隐私政策未明确排除将 API 输入用于模型训练，无公开 opt-out 机制 | 🔴 高 |
| **零留存缺失** | 无公开的企业级零留存 API 选项，无法保证推理后数据立即销毁 | 🔴 高 |
| **历史泄露事件** | 2025 年 1 月 Wiz Research 发现公开可访问的 ClickHouse 数据库，超 100 万条用户聊天记录和 API 密钥泄露 | 🔴 高 |
| **DDoS 防御不足** | 2025 年 1 月遭受 3.2Tbps DDoS 攻击，服务中断 48 小时 | 🟡 中 |
| **仿冒钓鱼** | 2024.12-2025.2 累计 2,650+ 个仿冒域名 | 🟡 中 |
| **思维链泄露** | DeepSeek-R1 的可见 CoT 可能包含检索过程中的敏感数据 | 🟡 中 |
| **Gartner 评估** | Gartner 不建议在涉及敏感业务数据或个人信息的情况下使用 DeepSeek API | 🔴 高 |

**三、对国企的特殊合规风险**

| 合规要求 | DeepSeek API 的问题 | 违规条款 |
|---------|-------------------|---------|
| 数据不出域 | L3+ 数据经 API 传输至 DeepSeek 服务器，违反"数据不出域"要求 | 《数据安全法》第 27 条 |
| 数据处理者责任 | 无法对 DeepSeek 侧的数据处理进行审计 | 《个人信息保护法》第 55 条 |
| 等保三级 | 数据留存和训练使用不透明，等保三级评估无法通过 | 等保 2.0 三级要求 |
| 算法备案 | DeepSeek 已完成备案，但企业作为下游使用方仍需自行备案 | 《生成式 AI 服务管理暂行办法》第 7 条 |
| 供应链安全 | DeepSeek 安全管理存在不确定性，缺乏透明的安全控制披露 | 《网络安全法》第 22 条 |

**四、如果必须使用 DeepSeek API 的防护措施**

| 防护层级 | 措施 | 效果 | 残余风险 |
|---------|------|------|---------|
| **数据脱敏** | API 调用前自动识别并脱敏 PII（姓名→张\*\*，金额→\*\*\*，工艺参数→\*\*\*） | 降低泄露影响 | 语义信息仍可能泄露 |
| **数据分级路由** | L3+ 数据 → 本地模型；L1-L2 数据 → API | 核心敏感数据不出域 | 需准确识别数据级别 |
| **API 网关管控** | 部署 API 网关，白名单控制出站请求，全量日志记录 | 可审计、可追溯 | 无法控制对方留存 |
| **Prompt 最小化** | RAG 检索时限制返回片段数量和长度，仅发送必要上下文 | 减少泄露面 | 可能影响纠错质量 |
| **专用 VPC** | 使用 DeepSeek 专业版 VPC 专属集群 + 数据驻留本地选项 | 数据隔离 | 需额外费用，且仍经 DeepSeek 基础设施 |

**五、DeepSeek API 使用场景分级**

| 场景 | 数据级别 | 是否允许使用 DeepSeek API | 替代方案 |
|------|---------|------------------------|---------|
| 查询公开法规标准 | L1（公开） | ✅ 允许，但建议优先本地模型 | 本地 Qwen3.6-27B |
| 标准条款对比验证 | L2（内部） | ⚠️ 有限允许，必须脱敏后调用 | 阿里云百炼 / 华为云昇腾 |
| 体系手册文本纠错 | L2-L3（内部-敏感） | ❌ 不允许，含企业内部管理信息 | 本地 Qwen3.6-27B |
| 财务数据审核 | L3（敏感） | ❌ 严禁 | 本地模型，数据不出内网 |
| 核心工艺参数校验 | L3+（机密） | ❌ 严禁 | 本地模型，数据不出内网 |
| 人事文件审查 | L3（敏感） | ❌ 严禁 | 本地模型，数据不出内网 |

> **最终建议**：对于国企，DeepSeek API 仅适用于**完全公开的非敏感查询场景**（如查询公开法规标准），且必须经过数据脱敏处理。**绝不能**用于包含企业敏感信息的纠错任务。推荐使用阿里云百炼或华为云昇腾作为 API 供应商，合规性更完善。

#### 2.5 3-5 年 TCO（总拥有成本）深度分析

##### 方案 A：纯私有化部署

| 费用项 | 第 1 年 | 第 2 年 | 第 3 年 | 第 4 年 | 第 5 年 | 5 年合计 |
|--------|--------|--------|--------|--------|--------|---------|
| 硬件采购（阶段二配置） | 10-14 万 | — | 升级 5-8 万 | — | — | 15-22 万 |
| 电费（0.8 元/度，8h×250 天） | 0.8-1.5 万 | 0.8-1.5 万 | 1.2-2 万 | 1.2-2 万 | 1.2-2 万 | 5.2-9 万 |
| 运维人力（兼职运维） | 1-2 万 | 1-2 万 | 1.5-3 万 | 1.5-3 万 | 1.5-3 万 | 6.5-13 万 |
| 硬件维保（3 年后） | — | — | — | 0.5-1 万 | 0.5-1 万 | 1-2 万 |
| 网络带宽（企业专线） | 0.5-1 万 | 0.5-1 万 | 0.5-1 万 | 0.5-1 万 | 0.5-1 万 | 2.5-5 万 |
| **年度合计** | **12.3-18.5 万** | **2.3-4.5 万** | **3.2-6 万** | **3.2-6 万** | **3.2-6 万** | **30-55 万** |

##### 方案 B：混合模式（本地 + API）

| 费用项 | 第 1 年 | 第 2 年 | 第 3 年 | 第 4 年 | 第 5 年 | 5 年合计 |
|--------|--------|--------|--------|--------|--------|---------|
| 硬件采购（阶段一配置） | 4-5.5 万 | — | 升级 3-5 万 | — | — | 7-10.5 万 |
| 电费 | 0.5-0.8 万 | 0.5-0.8 万 | 0.8-1.5 万 | 0.8-1.5 万 | 0.8-1.5 万 | 3.4-6.1 万 |
| API 调用费 | 0.3-0.5 万 | 0.5-1 万 | 0.8-1.5 万 | 1-2 万 | 1-2 万 | 3.6-7 万 |
| 运维人力 | 0.5-1 万 | 0.5-1 万 | 1-2 万 | 1-2 万 | 1-2 万 | 4-8 万 |
| 网络带宽 | 0.3-0.5 万 | 0.3-0.5 万 | 0.5-1 万 | 0.5-1 万 | 0.5-1 万 | 2.1-4 万 |
| **年度合计** | **5.6-7.8 万** | **1.8-3.3 万** | **6.1-11 万** | **3.3-6.5 万** | **3.3-6.5 万** | **20.1-36.6 万** |

##### 方案 C：纯 API 调用（无本地部署）

| 费用项 | 第 1 年 | 第 2 年 | 第 3 年 | 第 4 年 | 第 5 年 | 5 年合计 |
|--------|--------|--------|--------|--------|--------|---------|
| API 调用费 | 0.5-1 万 | 1-2 万 | 2-4 万 | 2-4 万 | 3-5 万 | 8.5-16 万 |
| 万悟平台云托管 | 2-3 万 | 2-3 万 | 2-3 万 | 2-3 万 | 2-3 万 | 10-15 万 |
| 网络带宽 | 0.3-0.5 万 | 0.3-0.5 万 | 0.5-1 万 | 0.5-1 万 | 0.5-1 万 | 2.1-4 万 |
| **年度合计** | **2.8-4.5 万** | **3.3-5.5 万** | **4.5-8 万** | **4.5-8 万** | **5.5-9 万** | **20.6-35 万** |

> ⚠️ **纯 API 方案不满足国企数据安全合规要求**，L3+ 数据禁止出境。此方案仅作成本参考。

##### 三种方案 5 年 TCO 对比

| 维度 | 方案 A：纯私有化 | 方案 B：混合模式 ✅ | 方案 C：纯 API |
|------|----------------|-------------------|---------------|
| **5 年总成本** | 30-55 万 | **20-37 万** | 21-35 万 |
| **数据安全合规** | ★★★★★ | ★★★★☆ | ★★☆☆☆（不合规） |
| **信创适配** | 需额外投入 | 可渐进适配 | 不适用 |
| **模型能力** | 受本地算力限制 | 本地+最强 API | 始终最强 |
| **扩展灵活性** | 需采购硬件 | 弹性扩展 | 完全弹性 |
| **首年投入** | 12-18 万 | **5-8 万** | 3-5 万 |

**结论**：**方案 B（混合模式）为最优选择**——5 年总成本最低（20-37 万），首年投入仅 5-8 万，数据安全合规可满足，扩展灵活。纯私有化方案 5 年成本最高但安全性最强；纯 API 方案成本看似低但**不满足合规要求**。

#### 2.6 GPU 服务器采购建议

##### RTX 4090D 服务器整机参考方案

| 配置 | GPU | CPU | 内存 | 存储 | 参考价格 | 适用阶段 |
|------|-----|-----|------|------|---------|---------|
| **入门单卡** | 1×RTX 4090D 24GB | Intel Xeon Gold 6430 (32C) | 64GB DDR5 ECC | 1TB NVMe SSD | 3-4 万 | 阶段一 |
| **生产双卡** | 2×RTX 4090D 24GB | 2×Intel Xeon Gold 6430 (64C) | 128GB DDR5 ECC | 2TB NVMe SSD | 6-8 万 | 阶段二 |
| **扩展四卡** | 4×RTX 4090D 24GB | 2×Intel Xeon Gold 6430 (64C) | 256GB DDR5 ECC | 4TB NVMe SSD | 12-18 万 | 阶段三 |
| **信创替代** | 1×昇腾 910B 64GB | 华为鲲鹏 920 (64C) | 128GB DDR4 ECC | 2TB NVMe SSD | 15-20 万 | 信创迁移 |

##### 采购注意事项

1. **选择涡轮散热版本**：服务器部署需选择涡轮（Turbo）散热版本的 RTX 4090D，支持机架式安装和 7×24 小时运行，风冷版本不适合服务器环境
2. **电源冗余**：单卡至少 850W，双卡需 1600W+，四卡需 2600W+，建议配置 1+1 冗余电源
3. **PCIe 带宽**：确保每张 GPU 独占 PCIe 4.0 x16 带宽，避免带宽共享导致性能下降
4. **ECC 内存**：服务器必须使用 ECC 内存，防止内存位翻转导致推理结果错误
5. **三年质保**：要求供应商提供三年上门质保服务，降低后期运维风险
6. **分期采购**：按阶段一→二→三渐进采购，每个阶段验证效果后再追加投入

---

### 三、敏感数据处理方案

#### 3.1 数据分级分类

| 数据等级 | 类型 | 示例 | 处理策略 |
|---------|------|------|---------|
| **L4-绝密** | 核心技术 | 锅炉设计图纸、核心工艺参数 | 禁止接入 AI，纯本地离线处理 |
| **L3-机密** | 财务/人事 | 财务报表、薪酬数据、员工信息 | 仅使用本地部署模型，数据不出服务器 |
| **L2-内部** | 业务数据 | 生产计划、质量记录、供应商信息 | 本地模型优先，脱敏后可使用 API 模型 |
| **L1-公开** | 公开信息 | 国家标准、法规文件、公开技术资料 | 可使用 API 模型 |

#### 3.2 敏感数据技术防护措施

```
┌──────────────────────────────────────────────────────┐
│                   数据安全防护体系                       │
│                                                        │
│  1. 网络隔离层                                          │
│     ├── 内外网物理/逻辑隔离                              │
│     ├── API 网关白名单控制                               │
│     └── 敏感服务仅内网可达                               │
│                                                        │
│  2. 数据传输层                                          │
│     ├── 全链路 HTTPS/TLS 加密                           │
│     ├── gRPC 服务间 mTLS 双向认证                       │
│     └── API 调用签名验证                                │
│                                                        │
│  3. 数据存储层                                          │
│     ├── 数据库字段级加密（财务/人事关键字段）              │
│     ├── MinIO 对象存储服务端加密(SSE-S3)                 │
│     ├── Elasticsearch 索引级权限控制                     │
│     └── Redis 敏感缓存加密存储                           │
│                                                        │
│  4. AI 处理层                                           │
│     ├── L3+ 数据仅走本地模型，禁止外发                    │
│     ├── 输入预处理：自动脱敏（姓名→张**，金额→***）       │
│     ├── 输出后处理：敏感词过滤（万悟安全护栏）             │
│     └── Prompt 注入防护                                 │
│                                                        │
│  5. 审计追溯层                                          │
│     ├── 全量操作日志（谁在何时访问了什么数据）             │
│     ├── AI 对话记录留存                                 │
│     └── 异常访问告警                                    │
└──────────────────────────────────────────────────────┘
```

#### 3.3 财务数据专项保护

1. **独立知识库**：财务相关知识库设置最高权限等级，仅财务角色可访问
2. **本地模型隔离**：财务 AI 助手绑定本地部署模型，API 模型不可选
3. **数据脱敏管道**：在 BFF 层实现自动脱敏中间件，对进入 AI 的财务数据自动遮蔽关键数字
4. **输出审计**：所有财务相关 AI 输出自动标记并记录审计日志
5. **万悟安全护栏**：配置财务敏感词表，防止财务数据泄露

#### 3.4 人事数据专项保护

1. **HR 专属知识库**：人事数据存储在独立知识库，权限仅限 HR 角色
2. **PII 识别与脱敏**：在文档导入知识库时自动识别并脱敏个人身份信息
3. **最小权限原则**：HR 助手仅能访问其权限范围内的数据
4. **对话隔离**：不同用户的 AI 对话完全隔离，不可交叉访问

---

### 四、权限体系设计

#### 4.1 组织架构映射

```
企业组织
├── 管理层
│   ├── 总经理办公室
│   └── 战略规划部
├── 技术中心
│   ├── 设计部
│   ├── 工艺部
│   └── 研发部
├── 生产制造
│   ├── 生产计划部
│   ├── 质量管理部
│   └── 各生产车间
├── 经营管理
│   ├── 财务部 ←── 高敏感
│   ├── 人力资源部 ←── 高敏感
│   └── 采购部
└── 体系管理
    ├── 体系办
    └── 安全环保部
```

#### 4.2 角色权限矩阵

| 角色 | 模型管理 | 知识库 | 智能体 | 工作流 | 安全护栏 | 权限管理 | API Key | 财务知识库 | 人事知识库 |
|------|---------|--------|--------|--------|---------|---------|---------|-----------|-----------|
| **超级管理员** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **体系管理员** | ❌ | ✅(体系) | ✅(体系) | ✅(体系) | ❌ | ❌ | ✅(体系) | ❌ | ❌ |
| **财务人员** | ❌ | ✅(财务) | ✅(财务) | ✅(财务) | ❌ | ❌ | ✅(财务) | ✅ | ❌ |
| **HR 人员** | ❌ | ✅(人事) | ✅(人事) | ✅(人事) | ❌ | ❌ | ✅(人事) | ❌ | ✅ |
| **技术工程师** | ❌ | ✅(技术) | ✅(技术) | ✅(技术) | ❌ | ❌ | ✅(技术) | ❌ | ❌ |
| **普通员工** | ❌ | ❌ | ✅(使用) | ✅(使用) | ❌ | ❌ | ❌ | ❌ | ❌ |
| **安全审计员** | ❌ | ✅(只读) | ✅(只读) | ✅(只读) | ✅ | ✅(只读) | ✅(只读) | ✅(只读) | ✅(只读) |

#### 4.3 万悟平台权限配置步骤

万悟平台内置 **组织 → 角色 → 用户** 三级权限体系（基于 Casbin RBAC），配置步骤：

1. **创建组织**：在「设置 → 组织管理」中创建各部门对应的组织
2. **创建角色**：在「设置 → 角色管理」中创建上述角色，分配功能权限
3. **创建用户**：在「设置 → 用户管理」中创建用户，分配至组织和角色
4. **知识库权限**：创建知识库时设置可见范围（公开/指定组织/指定角色/私密）
5. **应用权限**：发布智能体/工作流/RAG 应用时设置公开或私密
6. **API Key 权限**：API Key 继承创建者的权限范围

---

### 五、ERP/MES/WPS 集成方案

#### 5.1 集成架构

```
┌─────────────────────────────────────────────────────────┐
│                    万悟智能体平台                          │
│                                                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │
│  │  MCP Server  │  │  自定义工具   │  │  工作流节点   │      │
│  │  (标准协议)   │  │  (REST API)  │  │  (API 节点)  │      │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘      │
│         │                │                │              │
└─────────┼────────────────┼────────────────┼──────────────┘
          │                │                │
          ▼                ▼                ▼
   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
   │  MCP Gateway │  │  API Gateway │  │  Webhook    │
   └──────┬──────┘  └──────┬──────┘  └──────┬──────┘
          │                │                │
          ▼                ▼                ▼
   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
   │   ERP 系统   │  │   MES 系统   │  │  WPS 工作流  │
   │  SAP/用友等  │  │  制造执行系统  │  │  审批/协同   │
   └─────────────┘  └─────────────┘  └─────────────┘
```

#### 5.2 ERP 集成（以 SAP/用友为例）

**方式一：MCP Server 集成（推荐）**

在万悟平台创建自定义 MCP Server，封装 ERP 的 REST/OData API：

1. 在万悟「资源库 → MCP 服务」中创建自定义 MCP Server
2. 定义 MCP 工具（如 `query_purchase_order`、`get_inventory`、`submit_expense`）
3. 配置 ERP API 端点和认证信息
4. 在智能体中启用该 MCP Server
5. 用户通过自然语言调用 ERP 功能

**方式二：工作流 API 节点**

1. 在工作流画布中添加「API」节点
2. 配置 ERP API 的 URL、Method、Headers、Body
3. 将 API 返回结果传递给后续节点（大模型/条件分支等）
4. 实现复杂业务流程自动化

**典型 ERP 场景**：
- 采购订单查询与审批
- 库存实时查询
- 财务报表生成
- 供应商管理

#### 5.3 MES 集成

**方式一：MCP Server 集成**

创建 MES MCP Server，封装 MES API：
- `get_production_status`：查询生产进度
- `get_quality_report`：获取质量报告
- `report_anomaly`：上报异常
- `get_equipment_status`：设备状态查询

**方式二：实时数据推送**

1. MES 系统通过 Webhook 将关键事件推送到万悟工作流
2. 工作流自动触发 AI 分析和决策
3. 结果回写 MES 或通知相关人员

**典型 MES 场景**：
- 生产异常智能诊断
- 质量数据自动分析
- 设备预测性维护
- 工艺参数优化建议

#### 5.4 WPS/工作流集成

1. **WPS 文档智能处理**：通过 MCP Server 对接 WPS 开放 API
   - 文档自动生成
   - 合同智能审查
   - 报告自动排版

2. **审批流程集成**：
   - 万悟工作流作为审批辅助，提供 AI 建议但不替代审批
   - 审批意见自动生成
   - 合规性自动检查

3. **协同办公**：
   - 会议纪要自动生成
   - 邮件智能回复建议
   - 文档版本对比

#### 5.5 集成实施路径

| 阶段 | 时间 | 内容 |
|------|------|------|
| **第一阶段** | 1-2 月 | 万悟平台私有化部署 + 体系纠错场景落地 |
| **第二阶段** | 3-4 月 | ERP 查询类 MCP Server 开发（只读接口优先） |
| **第三阶段** | 5-6 月 | MES 数据查询 MCP Server + 质量分析智能体 |
| **第四阶段** | 7-8 月 | WPS 文档处理集成 + 审批辅助工作流 |
| **第五阶段** | 9-12 月 | ERP 写入类接口集成 + 全流程自动化 |

---

### 七、安全合规保障

#### 7.1 等保合规

| 要求 | 万悟平台支持 | 实施建议 |
|------|------------|---------|
| 身份鉴别 | ✅ JWT + OAuth + 验证码 | 启用 OAuth 对接企业 AD/LDAP |
| 访问控制 | ✅ RBAC 三级权限 | 按最小权限原则配置角色 |
| 安全审计 | ✅ 操作日志 | 对接企业日志平台 |
| 数据完整性 | ✅ mTLS + 签名 | 启用 gRPC mTLS |
| 数据保密性 | ✅ TLS + 字段加密 | 财务/人事数据加密存储 |
| 入侵防范 | ✅ 网络隔离 | 部署 WAF + 防火墙 |

#### 7.2 AI 安全专项

1. **Prompt 注入防护**：在 BFF 层实现输入清洗
2. **敏感词过滤**：利用万悟安全护栏功能，配置行业敏感词表
3. **输出审核**：AI 输出经过安全护栏过滤后再返回用户
4. **模型隔离**：不同敏感等级的数据使用不同的模型实例
5. **幻觉防控**：RAG 模式下要求引用来源，降低幻觉风险

---

## 第二部分：体系管理手册文本纠错场景 — 完整搭建步骤

---

### 一、场景分析

#### 1.1 业务痛点

锅炉制造企业的体系管理手册是基于体系管理文件（法规、国家标准、行业标准等）人工编写的，存在以下问题：

1. **引用错误**：引用了错误的标准编号或已废止的标准
2. **数据错误**：引用了错误的参数数据（如压力值、温度值等）
3. **法规虚构**：AI 或人工编造了不存在的法规条文
4. **语法错误**：语句不通顺、标点错误等
5. **逻辑错误**：前后矛盾、逻辑不自洽
6. **时效性问题**：引用的标准已更新但手册未同步

#### 1.2 解决思路

利用万悟平台的 **知识库 + RAG + 智能体/工作流** 能力，构建一个体系管理手册纠错系统：

```
┌──────────────────────────────────────────────────────────────┐
│                    手册纠错系统架构                              │
│                                                                │
│  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐  │
│  │  法规标准库    │     │  手册文档库    │     │  纠错结果库   │  │
│  │ (知识库-权威源) │     │ (待纠错文档)  │     │ (纠错报告)   │  │
│  └──────┬───────┘     └──────┬───────┘     └──────┬───────┘  │
│         │                     │                     │          │
│         ▼                     ▼                     │          │
│  ┌──────────────────────────────────────────┐      │          │
│  │           纠错智能体 / 纠错工作流           │      │          │
│  │                                            │      │          │
│  │  1. 逐段提取手册内容                        │      │          │
│  │  2. 识别引用的标准/法规/数据                │      │          │
│  │  3. RAG 检索法规标准库验证引用              │      │          │
│  │  4. 联网检索验证最新标准状态                │      │          │
│  │  5. 语法/逻辑检查                          │      │          │
│  │  6. 生成纠错报告                           │      │          │
│  └──────────────────────────────────────────┘      │          │
│                                                     │          │
│  ┌──────────────────────────────────────────┐      │          │
│  │           本地部署模型（私有化）             │      │          │
│  │     Qwen3.6-27B / GLM-5-Turbo 等           │      │          │
│  └──────────────────────────────────────────┘      │          │
└──────────────────────────────────────────────────────┘
```

---

### 二、完整搭建步骤

#### 步骤一：平台部署（1-2 天）

**1.1 基础环境准备**

```bash
# 服务器最低配置（验证阶段）
# CPU: 32C, RAM: 64GB, GPU: 1×RTX 4090D 24GB, SSD: 1TB

# 安装 Docker & Docker Compose
curl -fsSL https://get.docker.com | sh
systemctl enable docker && systemctl start docker

# 克隆项目
git clone https://github.com/UnicomAI/wanwu.git
cd wanwu
```

**1.2 配置环境变量**

```bash
cp .env.bak .env

# 编辑 .env 文件
vi .env

# 必须修改的配置：
WANWU_ARCH=amd64
WANWU_EXTERNAL_IP=<你的服务器IP>
WANWU_BFF_JWT_SIGNING_KEY=<自定义复杂随机字符串>

# 数据库选择（推荐 MySQL，信创环境可选 TiDB/OceanBase）
WANWU_DB_NAME=mysql
```

**1.3 启动平台**

```bash
docker network create wanwu-net
docker compose --env-file .env --env-file .env.image.amd64 up -d

# 等待所有服务健康启动（约 3-5 分钟）
docker compose --env-file .env --env-file .env.image.amd64 ps

# 访问 http://<服务器IP>:8081
# 默认账号: admin / Wanwu123456
```

**1.4 部署本地模型**

```bash
# 方式一：使用 Ollama 部署本地模型
docker run -d --gpus all \
  -v /data/ollama:/root/.ollama \
  -p 11434:11434 \
  --name ollama \
  ollama/ollama

# 拉取模型（推荐 Qwen3.6-27B 或 GLM-5-Turbo）
# Qwen3.6-27B：27B 稠密模型，Q4_K_M 量化仅需约 17GB 显存，RTX 4090D 24GB 完美适配
docker exec ollama ollama pull qwen3.6:27b

# GLM-5-Turbo：GLM-5.1 的轻量推理版，适合 24GB 显存消费级显卡
docker exec ollama ollama pull glm5-turbo

# 拉取 Embedding 模型（用于 RAG 向量化）
docker exec ollama ollama pull bge-m3
```

**模型选型说明**

| 模型 | 参数量 | 架构 | 24GB 显存适配 | 推荐量化 | 推理速度 | 核心优势 |
|------|--------|------|-------------|---------|---------|---------|
| **Qwen3.6-27B** | 27B | 稠密(Dense) | ✅ 完美适配 | Q4_K_M (~17GB) | 10-20 t/s | 编程/推理/中文能力极强，SWE-bench Verified 77.2%，Apache 2.0 开源 |
| **GLM-5-Turbo** | ~40B 激活 | MoE(754B 总参) | ✅ 可运行 | 4-bit 量化 | 8-15 t/s | 编程能力达 Claude Opus 94.6%，MIT 开源，长程自主工程能力 |
| **Qwen3.6-35B-A3B** | 35B/3B 激活 | MoE | ✅ 完美适配 | Q4_K_M (~24GB) | 20-34 t/s | MoE 架构推理极快，适合高并发场景 |

> **重要提示**：GLM-5.1 完整版（754B 参数）需 8×A100 80GB 才能运行，RTX 4090D 仅能运行其轻量版 GLM-5-Turbo。若需 GLM-5.1 满血版能力，建议通过 API 调用。

**1.5 在万悟平台导入模型**

1. 登录万悟平台 → 「模型管理」→「添加模型」
2. 导入 LLM：
   - 模型名称：`qwen3.6:27b`
   - 模型类型：LLM
   - 供应商：Ollama
   - 推理 URL：`http://<服务器IP>:11434`
3. 导入 Embedding：
   - 模型名称：`bge-m3`
   - 模型类型：Embedding
   - 供应商：Ollama
   - 推理 URL：`http://<服务器IP>:11434`

---

#### 步骤二：构建法规标准知识库（2-3 天）

**2.1 创建法规标准知识库**

1. 进入「知识库」→「创建知识库」
2. 配置：
   - 名称：`锅炉法规标准库`
   - 描述：`包含锅炉行业相关法规、国家标准、行业标准的权威知识库`
   - Embedding 模型：选择已导入的 `bge-m3`
   - 权限：仅体系管理角色可见

**2.2 上传法规标准文档**

需要收集并上传以下类型的文档：

| 文档类型 | 示例 | 来源 |
|---------|------|------|
| **国家法规** | 《特种设备安全法》《锅炉安全技术规程》 | 国家法律法规数据库 |
| **国家标准** | GB/T 16507《水管锅炉》、GB/T 16508《锅壳锅炉》 | 国家标准全文公开系统 |
| **行业标准** | NB/T 47034《工业锅炉技术条件》 | 行业标准发布机构 |
| **安全规程** | TSG 11《锅炉安全技术规程》 | 市场监管总局 |
| **企业标准** | 公司内部体系管理文件 | 体系管理部 |

**2.3 文档解析配置**

1. 上传文档时选择解析方式：
   - **普通文档**：默认 OCR 解析
   - **含表格/公式文档**：选择 MinerU 模型解析（需额外部署 [DocParserServer](https://github.com/UnicomAI/DocParserServer)）
2. 分段设置：
   - 推荐使用**父子分段**模式
   - 父段长度：1000 字符
   - 子段长度：300 字符
   - 重叠长度：50 字符
3. 等待文档解析完成（状态变为"已完成"）

**2.4 配置关键词标签**

对关键法规标准文档设置关键词标签，提升检索召回率：

- 标签示例：`锅炉`、`压力容器`、`安全阀`、`GB/T 16507`、`TSG 11`
- 在知识库文档列表中，对每个分段可设置关键词

**2.5 命中测试**

1. 在知识库详情页点击「命中测试」
2. 输入测试查询，如：`锅炉安全技术规程对安全阀的要求`
3. 验证检索结果是否准确返回相关法规条文
4. 根据结果调整分段策略和关键词

---

#### 步骤三：创建纠错智能体（1-2 天）

提供两种实现方式，可根据需求选择或组合使用：

##### 方式 A：智能体模式（适合交互式纠错）

**3A.1 创建智能体**

1. 进入「智能体」→「创建智能体」
2. 基本配置：
   - 名称：`体系手册纠错助手`
   - 描述：`基于法规标准库对体系管理手册进行纠错，检测引用错误、数据错误、法规虚构、语法错误和逻辑错误`
   - 模型：选择本地部署的 `qwen3.6:27b` 或 `glm5-turbo`

**3A.2 编写系统提示词**

```
你是一个专业的锅炉制造行业体系管理手册纠错专家。你的任务是对手册内容进行严格的审查和纠错。

## 你的职责

1. **引用验证**：检查手册中引用的标准编号、法规名称是否正确且现行有效
2. **数据核实**：检查手册中引用的技术参数（压力、温度、尺寸等）是否与标准原文一致
3. **法规真实性**：确认引用的法规条文确实存在，不是虚构的
4. **语法检查**：检查语句是否通顺、标点是否正确、术语是否规范
5. **逻辑一致性**：检查前后文是否存在矛盾，逻辑是否自洽
6. **时效性检查**：检查引用的标准是否为最新版本，是否已被废止或替代

## 工作流程

1. 仔细阅读用户提交的手册内容
2. 识别其中所有引用的标准、法规、技术参数
3. 基于知识库中的法规标准进行逐一验证
4. 对每个发现的问题给出：
   - 错误类型（引用错误/数据错误/法规虚构/语法错误/逻辑错误/时效性问题）
   - 错误位置（原文引用）
   - 正确内容（基于权威来源的修正建议）
   - 依据来源（具体的标准编号和条款号）
5. 生成结构化的纠错报告

## 输出格式

请按以下格式输出纠错结果：

### 纠错报告

**文档名称**：[文档名]
**审查日期**：[日期]
**发现问题数**：[数量]

---

#### 问题 #1
- **错误类型**：[引用错误/数据错误/法规虚构/语法错误/逻辑错误/时效性问题]
- **原文内容**：「[错误原文]」
- **问题描述**：[具体说明]
- **正确内容**：「[修正后内容]」
- **依据来源**：[标准编号/条款号]

---

#### 问题 #2
...

## 注意事项

- 只报告确定的问题，对不确定的内容标注"建议人工复核"
- 严格基于知识库中的权威资料进行判断，不要凭空推测
- 如果知识库中没有相关标准信息，明确告知用户需要人工核实
- 技术参数的纠错必须给出精确数值和来源
```

**3A.3 关联知识库**

1. 在智能体配置中，点击「知识库」
2. 选择 `锅炉法规标准库`
3. 检索模式：**混合检索**（向量 + 全文）
4. Rerank：开启（如有 Rerank 模型）

**3A.4 启用联网检索（可选）**

1. 在智能体配置中，开启「联网检索」
2. 用于验证标准的最新状态（是否已废止/更新）
3. 配置搜索服务商（博查/Tavily 等，需在 Callback 中配置）

**3A.5 测试纠错**

在智能体调试界面输入测试内容：

```
请审查以下手册内容：

根据《锅炉安全技术监察规程》（TSG G0001-2012）的要求，额定蒸汽压力大于
3.8MPa的锅炉，其安全阀的排放能力应当大于锅炉的最大连续蒸发量。同时参照
GB/T 16507.1-2013《水管锅炉 第1部分：总则》，锅炉受压元件的计算压力应
取设计压力的1.1倍。
```

预期纠错结果应包括：
- TSG G0001-2012 已被 TSG 11 替代（时效性问题）
- 具体参数需与标准原文核对（数据核实）

##### 方式 B：工作流模式（适合批量纠错）

**3B.1 创建工作流**

1. 进入「工作流」→「创建工作流」
2. 设计以下工作流节点：

```
[开始] → [知识库检索] → [大模型分析] → [条件判断] → [纠错报告生成] → [结束]
                                    ↓
                              [联网检索]（可选，验证标准时效性）
```

**3B.2 节点配置**

**节点 1：开始节点**
- 输入变量：`manual_content`（手册内容文本）

**节点 2：知识库检索节点**
- 知识库：`锅炉法规标准库`
- 检索模式：混合检索
- 查询内容：`{{manual_content}}`
- Top-K：10

**节点 3：大模型分析节点**
- 模型：`qwen3.6:27b`
- 系统提示词：同方式 A 中的提示词
- 用户消息模板：
```
请审查以下手册内容，并结合检索到的法规标准进行纠错：

## 手册内容
{{manual_content}}

## 检索到的相关法规标准
{{knowledge_result}}
```

**节点 4：条件判断节点**
- 条件：如果发现引用了标准编号，则进入联网检索验证时效性
- 否则直接进入报告生成

**节点 5：联网检索节点（可选）**
- 搜索关键词：提取的标准编号 + "最新版本"/"废止"
- 将搜索结果补充到大模型上下文

**节点 6：纠错报告生成节点**
- 大模型节点
- 整合所有信息，生成最终结构化纠错报告

**3B.3 测试工作流**

1. 在工作流调试界面输入测试手册内容
2. 逐步检查各节点输出
3. 调优检索参数和提示词

---

#### 步骤四：配置安全护栏（0.5 天）

**4.1 创建敏感词表**

1. 进入「安全护栏」→「创建敏感词表」
2. 名称：`体系纠错安全策略`
3. 添加输入敏感词：
   - 防止用户输入与纠错无关的敏感指令
   - 防止 Prompt 注入攻击
4. 添加输出敏感词：
   - 防止模型输出未经核实的修改建议
   - 添加免责声明关键词

**4.2 绑定到智能体/工作流**

在智能体或工作流配置中，选择绑定的安全护栏为 `体系纠错安全策略`。

---

#### 步骤五：权限配置与发布（0.5 天）

**5.1 配置组织与角色**

1. 「设置」→「组织管理」→ 创建 `体系管理部` 组织
2. 「设置」→「角色管理」→ 创建 `体系管理员` 角色，分配知识库和智能体权限
3. 「设置」→「用户管理」→ 将体系管理人员加入对应组织和角色

**5.2 设置知识库权限**

1. `锅炉法规标准库`：设置为 `体系管理部` 可见
2. 确保财务/人事等其他部门无法访问

**5.3 发布纠错应用**

1. 智能体/工作流调试完成后，点击「发布」
2. 发布方式选择 `私密`（仅体系管理部可用）
3. 可选：发布为 API，供其他系统调用

---

#### 步骤六：API 集成（可选，1-2 天）

如需将纠错能力集成到现有 OA/WPS 系统中：

**6.1 获取 API Key**

1. 进入「API Key 管理」→「创建 API Key」
2. 关联纠错智能体/工作流

**6.2 调用示例**

```python
import requests

API_BASE = "http://<服务器IP>:8081"
API_KEY = "your-api-key"

# 调用智能体纠错
response = requests.post(
    f"{API_BASE}/v1/agent/chat",
    headers={
        "Authorization": f"Bearer {API_KEY}",
        "Content-Type": "application/json"
    },
    json={
        "agent_id": "your-agent-id",
        "message": "请审查以下手册内容：...",
        "stream": False
    }
)

result = response.json()
print(result)
```

**6.3 批量纠错脚本**

```python
import os
import requests

API_BASE = "http://<服务器IP>:8081"
API_KEY = "your-api-key"
MANUAL_DIR = "/path/to/manuals"
REPORT_DIR = "/path/to/reports"

for filename in os.listdir(MANUAL_DIR):
    if filename.endswith(('.txt', '.md')):
        with open(os.path.join(MANUAL_DIR, filename), 'r') as f:
            content = f.read()

        response = requests.post(
            f"{API_BASE}/v1/agent/chat",
            headers={
                "Authorization": f"Bearer {API_KEY}",
                "Content-Type": "application/json"
            },
            json={
                "agent_id": "your-agent-id",
                "message": f"请审查以下手册内容：\n\n{content}",
                "stream": False
            }
        )

        report = response.json()
        report_path = os.path.join(REPORT_DIR, f"纠错报告_{filename}")
        with open(report_path, 'w') as f:
            f.write(report.get('data', {}).get('answer', ''))
```

---

#### 步骤七：持续优化（长期）

**7.1 知识库更新机制**

| 触发条件 | 操作 | 责任人 |
|---------|------|--------|
| 国家标准更新 | 下载新版标准文档 → 上传至知识库 → 删除旧版 | 体系管理员 |
| 法规修订 | 同上 | 体系管理员 |
| 企业内控制度变更 | 更新内部文件 → 重新上传 | 体系管理员 |
| 每年定期审查 | 全面检查知识库文档时效性 | 体系管理员 |

**7.2 纠错效果评估**

| 指标 | 目标值 | 评估方式 |
|------|--------|---------|
| 引用错误检出率 | ≥ 90% | 人工标注测试集 |
| 数据错误检出率 | ≥ 85% | 人工标注测试集 |
| 误报率 | ≤ 10% | 统计纠错结果中错误报告的比例 |
| 法规虚构检出率 | ≥ 95% | 构造含虚构法规的测试用例 |

**7.3 模型优化方向**

1. **Prompt 迭代**：根据实际纠错效果持续优化提示词
2. **Few-shot 示例**：在提示词中加入典型纠错案例
3. **模型升级**：随着更强开源模型发布（如 Qwen3.6-27B → 后续版本），升级本地模型
4. **RAG 调优**：调整分段策略、检索参数、Rerank 策略
5. **GraphRAG**：对复杂跨文档引用场景，启用知识图谱增强检索
6. **混合推理**：敏感数据走本地 Qwen3.6-27B，复杂推理任务走 API（GLM-5.1/Qwen3.6-Max）

---

### 三、实施时间表

| 阶段 | 时间 | 交付物 |
|------|------|--------|
| 平台部署 + 模型导入 | 第 1 周 | 可用的万悟平台 + 本地模型 |
| 法规标准知识库建设 | 第 2 周 | 完整的法规标准知识库 |
| 纠错智能体/工作流开发 | 第 3 周 | 可用的纠错应用 |
| 安全护栏 + 权限配置 | 第 3 周 | 安全策略 + 权限体系 |
| 测试验证 + 调优 | 第 4 周 | 纠错效果达标 |
| API 集成（可选） | 第 5 周 | OA/WPS 集成接口 |
| 正式上线 + 培训 | 第 6 周 | 用户手册 + 培训完成 |

---

### 四、扩展场景：从文本纠错到全厂 AI 化

> **核心问题**：万悟平台能否承载焊接质检（CV）、生产参数优化（ML）等扩展需求？
>
> **核心结论**：万悟平台**不能单独完成**所有工作，但可以作为**统一 AI 编排层**，通过 MCP 工具集成和 API 对接，将专业子系统串联为一个整体。推荐"1+N"架构：1 个万悟平台 + N 个专业子系统。

#### 4.1 万悟平台能力边界分析

| 能力 | 万悟平台支持情况 | 说明 |
|------|----------------|------|
| LLM 文本推理 | ✅ 原生支持 | Qwen3.6-27B / GLM-5-Turbo 本地推理 |
| RAG 知识库检索 | ✅ 原生支持 | 向量化 + 语义检索 + GraphRAG |
| Agent 工作流 | ✅ 原生支持 | 多步骤编排、工具调用 |
| MCP 工具集成 | ✅ 原生支持 | 可通过 MCP 协议对接外部系统 |
| 图片上传 | ✅ 前端支持 | uploadImg 组件 |
| 文生图模型 | ✅ 部分支持 | 已集成元境等文生图模型 |
| **CV 模型训练** | ❌ 不支持 | 无 YOLOv8/图像分类训练流水线 |
| **数据标注** | ❌ 不支持 | 无图像标注/审核工作台 |
| **模型微调** | ❌ 不支持 | 无 LoRA/全量微调训练框架 |
| **时序数据分析** | ❌ 不支持 | 无传感器数据处理/特征工程 |
| **参数优化** | ❌ 不支持 | 无优化求解器/推荐引擎 |
| **IoT 数据采集** | ❌ 不支持 | 无设备数据接入管道 |

#### 4.2 "1+N" 统一 AI 平台架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                        统一 AI 平台（"1+N" 架构）                      │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │                    万悟平台（统一 AI 编排层）                      │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │ │
│  │  │ 文本纠错  │  │ 法规查询  │  │ 知识问答  │  │ 跨系统调度   │   │ │
│  │  │ Agent    │  │ Agent    │  │ Agent    │  │ Agent        │   │ │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────────┘   │ │
│  │                         │                                        │ │
│  │              MCP 工具协议 / REST API                              │ │
│  └─────────────┬───────────┬──────────────┬────────────────────────┘ │
│                │           │              │                          │
│  ┌─────────────▼──┐  ┌────▼─────────┐  ┌─▼──────────────┐          │
│  │  子系统 N1：     │  │  子系统 N2：   │  │  子系统 N3：     │          │
│  │  视觉质检平台    │  │  参数优化平台   │  │  设备预测维护    │          │
│  │                │  │              │  │              │          │
│  │ · 图片采集      │  │ · IoT 数据采集 │  │ · 振动/温度    │          │
│  │ · 数据标注      │  │ · 特征工程     │  │   传感器数据   │          │
│  │ · YOLOv8 训练   │  │ · 参数优化     │  │ · 异常检测     │          │
│  │ · 边缘推理部署  │  │ · 参数推荐     │  │ · 寿命预测     │          │
│  └────────────────┘  └──────────────┘  └──────────────┘          │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │                    共享基础设施层                                  │ │
│  │  GPU 服务器（RTX 4090D）  │  数据库集群  │  对象存储  │  消息队列  │ │
│  └─────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

#### 4.3 扩展场景 N1：焊接质检视觉平台

##### 业务需求

- 拍摄焊接部位照片，自动识别是否达标（良好/不良/缺陷）
- 支持图片标注后训练模型，持续提升检测精度
- 实时或准实时反馈，减少人工目检依赖

##### 技术方案

| 环节 | 推荐方案 | 说明 |
|------|---------|------|
| **图片采集** | 工业相机 + HTTP 上传 | 产线工位部署工业相机，拍照后上传至平台 |
| **数据标注** | Label Studio（开源） | 支持 YOLO/分割/分类标注，团队协作，审核流程 |
| **模型训练** | YOLOv8（Ultralytics） | 工业缺陷检测主流方案，mAP@0.5 可达 90%+ |
| **模型优化** | Optuna 超参优化 | 自动搜索最优训练参数，提升模型精度 |
| **推理部署** | TensorRT 加速 + FastAPI | RTX 4090D 单图推理 < 20ms，支持 API 调用 |
| **边缘部署** | ONNX Runtime / Jetson | 轻量化模型可部署到产线工控机，离线推理 |

##### 焊接缺陷检测模型选型

| 模型 | 参数量 | 推理速度 | mAP@0.5 | 显存占用 | 适用场景 |
|------|--------|---------|---------|---------|---------|
| **YOLOv8n** | 3.2M | ~100 FPS | ~85% | ~1.5GB | 边缘部署、实时检测 |
| **YOLOv8s** | 11.2M | ~60 FPS | ~89% | ~3GB | 服务器推理、精度优先 |
| **YOLOv8m** | 25.9M | ~35 FPS | ~92% | ~6GB | 高精度场景 |
| **YOLOv8l** | 43.7M | ~20 FPS | ~93% | ~9GB | 离线分析、最高精度 |

> **推荐**：YOLOv8s 作为起步模型（精度与速度均衡），RTX 4090D 可同时运行 LLM + YOLOv8s。

##### 与万悟平台集成方式

```
用户在万悟平台对话：
  "帮我检查这批焊接照片是否合格"
       │
       ▼
万悟 Agent 识别意图 → 调用 MCP 工具 "welding_inspect"
       │
       ▼
MCP 工具 → 调用视觉质检平台 API → YOLOv8 推理
       │
       ▼
返回结果 → 万悟 Agent 整理为自然语言报告
  "本次检测 50 张图片，其中 3 张存在缺陷：
   - 图片 #12：气孔缺陷（置信度 92%）
   - 图片 #23：未熔合（置信度 88%）
   - 图片 #41：焊瘤（置信度 95%）
   建议对以上焊缝进行返工处理。"
```

##### 实施步骤与成本

| 阶段 | 工作内容 | 周期 | 额外成本 |
|------|---------|------|---------|
| 数据采集 | 采集焊接图片 1000+ 张，覆盖良好/不良/缺陷 | 2-4 周 | 工业相机 0.5-2 万 |
| 数据标注 | 使用 Label Studio 标注，3 类目标 | 1-2 周 | 0 元（开源） |
| 模型训练 | YOLOv8s 训练 + Optuna 优化 | 1 周 | 0 元（复用 4090D） |
| 推理服务 | FastAPI + TensorRT 部署 | 1 周 | 0 元 |
| 万悟集成 | 开发 MCP 工具对接 | 1 周 | 0 元 |
| **合计** | — | **6-9 周** | **0.5-2 万**（仅工业相机） |

#### 4.4 扩展场景 N2：生产参数优化平台

##### 业务需求

- 采集设备运行数据（电流、电压、速度、温度等）
- 分析熟练工 vs 新手的参数差异，提取最优参数组合
- 向工人推荐最优参数，降低操作门槛

##### 技术方案

| 环节 | 推荐方案 | 说明 |
|------|---------|------|
| **数据采集** | IoT 网关 + MQTT/Kafka | 从 PLC/传感器采集实时数据 |
| **数据存储** | TimescaleDB / InfluxDB | 时序数据库，高效存储传感器数据 |
| **特征工程** | Pandas + Scikit-learn | 提取参数统计特征、时序特征 |
| **参数优化** | Optuna / 贝叶斯优化 | 基于历史数据搜索最优参数组合 |
| **参数推荐** | 规则引擎 + ML 模型 | 根据工况条件推荐最优参数 |
| **可视化** | Grafana 仪表盘 | 实时展示设备状态和参数推荐 |

##### 参数优化技术路线

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  数据采集层    │     │  分析优化层    │     │  推荐应用层    │
│              │     │              │     │              │
│ PLC/传感器    │     │ 特征提取      │     │ 参数推荐      │
│   ↓          │     │  ↓           │     │  ↓           │
│ IoT 网关     │ ──→ │ 聚类分析      │ ──→ │ 工况匹配      │
│   ↓          │     │  ↓           │     │  ↓           │
│ MQTT/Kafka   │     │ 贝叶斯优化    │     │ 实时推送      │
│   ↓          │     │  ↓           │     │  ↓           │
│ 时序数据库    │     │ 最优参数集    │     │ 万悟 Agent    │
└──────────────┘     └──────────────┘     └──────────────┘
```

**核心算法**：

1. **聚类分析**：将历史参数按质量结果聚类，识别"优质参数区间"
2. **贝叶斯优化**：在参数空间中搜索使质量指标最优的参数组合
3. **工况匹配**：根据当前工况（材料/板厚/位置）匹配最优参数

##### 与万悟平台集成方式

```
用户在万悟平台对话：
  "当前焊接 Q345B 钢板，板厚 12mm，推荐什么参数？"
       │
       ▼
万悟 Agent 识别意图 → 调用 MCP 工具 "param_recommend"
       │
       ▼
MCP 工具 → 查询参数优化平台 → 匹配工况 + 返回最优参数
       │
       ▼
返回结果 → 万悟 Agent 整理为自然语言
  "根据历史数据分析，Q345B 钢板 12mm 对接焊推荐参数：
   - 焊接电流：180-200A
   - 焊接电压：24-26V
   - 焊接速度：25-30 cm/min
   - 气体流量：15-18 L/min
   以上参数基于 326 条优质焊接记录统计得出，合格率 97.2%。"
```

##### 实施步骤与成本

| 阶段 | 工作内容 | 周期 | 额外成本 |
|------|---------|------|---------|
| 数据采集 | 部署 IoT 网关，接入 PLC/传感器 | 2-3 周 | IoT 网关 1-3 万/产线 |
| 数据平台 | 部署 TimescaleDB + Kafka + Grafana | 1-2 周 | 0 元（开源，复用服务器） |
| 算法开发 | 特征工程 + 聚类 + 贝叶斯优化 | 2-3 周 | 0 元 |
| 推荐服务 | FastAPI 推理服务 + 工况匹配 | 1-2 周 | 0 元 |
| 万悟集成 | 开发 MCP 工具对接 | 1 周 | 0 元 |
| **合计** | — | **7-11 周** | **1-3 万**（IoT 网关） |

#### 4.5 各方案对比：统一平台 vs 独立子系统

| 对比维度 | 方案 A：万悟平台独揽 | 方案 B："1+N"架构 ✅ | 方案 C：全部外购商业方案 |
|---------|-------------------|-------------------|---------------------|
| **可行性** | ❌ 不可行，万悟不支持 CV 训练和 IoT | ✅ 可行，各子系统专业高效 | ✅ 可行，但成本高 |
| **开发成本** | — | 低（0.5-5 万额外硬件） | 高（50-200 万/年） |
| **数据安全** | — | ★★★★★ 全部本地部署 | ★★★☆☆ 依赖供应商 |
| **扩展性** | 差 | ✅ 好，按需增加子系统 | 好，但需付费 |
| **用户体验** | 统一入口 | ✅ 统一入口（万悟对话） | 多系统切换 |
| **维护复杂度** | 低 | 中（需维护 N 个子系统） | 低（供应商维护） |
| **信创适配** | — | ✅ 全栈可控 | 依赖供应商 |

#### 4.6 共享基础设施：GPU 服务器资源复用与训练调度

> **核心原则**：推理是常驻服务，训练是批处理任务。两者分时复用 GPU，互不干扰。

##### 训练 vs 推理的资源特征

| 特征 | 推理（Inference） | 训练（Training） |
|------|-----------------|----------------|
| **运行模式** | 常驻服务，7×24 在线 | 批处理任务，一次性或周期性 |
| **时间要求** | 实时响应（< 1s） | 无实时要求（小时~天级） |
| **频率** | 每天持续 | 初次训练 1 次 + 定期重训（月/季度） |
| **GPU 占用** | 持续占用显存 | 训练时占用，完成后释放 |
| **可中断性** | 不可中断 | 可随时暂停/恢复 |

##### GPU 分时复用策略

```
┌─────────────────────────────────────────────────────────────────┐
│              RTX 4090D 24GB 分时复用策略                          │
│                                                                   │
│  日间模式（8:00-20:00）          夜间模式（20:00-8:00）            │
│  ┌───────────────────────┐     ┌───────────────────────┐        │
│  │ 推理服务（常驻）        │     │ 训练任务（批处理）      │        │
│  │                       │     │                       │        │
│  │ LLM 推理    17GB      │     │ YOLOv8 训练  6GB      │        │
│  │ YOLOv8 推理   3GB     │     │ LLM 微调     8GB      │        │
│  │ 余量          4GB     │     │ 参数优化     1GB      │        │
│  │                       │     │ 余量         9GB      │        │
│  └───────────────────────┘     └───────────────────────┘        │
│                                                                   │
│  周末模式：全天可运行训练任务，推理服务按需启动                      │
│  节假日模式：全天训练，充分利用闲置算力                              │
└─────────────────────────────────────────────────────────────────┘
```

##### 训练任务调度表

| 训练任务 | 首次训练时间 | 重训周期 | 调度时段 | GPU 占用 | 说明 |
|---------|------------|---------|---------|---------|------|
| YOLOv8s 焊接质检 | 2-4 小时 | 每月（新数据积累后） | 夜间/周末 | ~6GB | 新缺陷类型出现时需重训 |
| YOLOv8n 其他质检 | 1-2 小时 | 每季度 | 夜间/周末 | ~3GB | 不同产品线的质检模型 |
| LLM LoRA 微调 | 4-8 小时 | 每季度 | 周末 | ~8GB | 针对企业术语/纠错风格微调 |
| 参数优化搜索 | 2-6 小时 | 每月 | 夜间 | ~1GB | 新工况数据积累后重训 |
| 其他 ML 模型 | 视具体而定 | 视具体而定 | 夜间/周末 | 视具体而定 | 未来新需求 |

> **关键洞察**：训练是低频批处理任务，不需要为训练单独采购 GPU。通过日间推理 + 夜间训练的分时复用，单卡 RTX 4090D 即可满足当前所有需求。当训练任务排队严重时，再考虑增加第二张 GPU。

##### 自动化训练流水线

```bash
# 训练调度示例（cron + 脚本）

# 每月 1 日凌晨 2:00 启动 YOLOv8 重训
0 2 1 * * /opt/ai-pipeline/scripts/train_yolo.sh >> /var/log/ai-training.log 2>&1

# 每季度首月 1 日凌晨 2:00 启动 LLM LoRA 微调
0 2 1 1,4,7,10 * /opt/ai-pipeline/scripts/train_llm_lora.sh >> /var/log/ai-training.log 2>&1

# 训练完成后自动切换回推理模式
# train_yolo.sh 末尾：
#   1. 训练模型 → 验证 mAP > 阈值 → 替换线上模型
#   2. 失败则保留旧模型，发送告警通知
#   3. 记录训练日志和模型版本
```

#### 4.7 全公司 AI 需求收集与优先级评估

> **问题**：不止体系管理部有 AI 需求，其他部门（生产、质检、采购、安全等）也有大量潜在需求。如何系统性地收集、评估和排期？

##### 4.7.1 AI 需求收集方法论

**第一步：部门访谈模板**

对每个部门进行结构化访谈，使用以下模板收集需求：

| 维度 | 访谈问题 | 目的 |
|------|---------|------|
| **业务痛点** | 当前哪些工作最耗时、最易出错、最依赖经验？ | 识别高价值场景 |
| **数据基础** | 有哪些电子化数据？格式是什么？数据量多大？ | 评估技术可行性 |
| **决策场景** | 哪些决策需要查阅大量资料或对比多个方案？ | 适合 LLM/RAG 场景 |
| **视觉场景** | 哪些工作依赖人工目视检查或图片判断？ | 适合 CV 场景 |
| **数据场景** | 哪些工作依赖经验调参或数据分析？ | 适合 ML/优化场景 |
| **安全等级** | 涉及的数据属于什么级别（L1-L3+）？ | 决定本地 vs API |
| **紧迫程度** | 这个问题不解决会怎样？有没有替代方案？ | 评估优先级 |

**第二步：需求分类矩阵**

将收集到的需求按技术类型分类：

| 技术类型 | 典型场景 | 万悟平台支持 | 需要子系统 | 示例 |
|---------|---------|------------|----------|------|
| **A 类：LLM+RAG** | 文档查询/纠错/生成 | ✅ 原生支持 | 否 | 体系手册纠错、合同审查、制度查询 |
| **B 类：LLM+工具** | 调用外部系统完成操作 | ✅ Agent+MCP | 视情况 | ERP 查询、报表生成、邮件起草 |
| **C 类：CV 检测** | 图片/视频缺陷识别 | ❌ 需子系统 | YOLOv8 平台 | 焊接质检、表面缺陷、包装检查 |
| **D 类：ML 优化** | 参数推荐/预测/分类 | ❌ 需子系统 | ML 平台 | 焊接参数优化、能耗预测、良率分析 |
| **E 类：NLP 结构化** | 表格提取/信息抽取 | ✅ LLM 可实现 | 否 | 标准条款提取、报价单解析 |

**第三步：优先级评估打分卡**

对每个需求按以下维度打分（1-5 分），计算综合优先级：

| 维度 | 权重 | 1 分 | 3 分 | 5 分 |
|------|------|------|------|------|
| **业务价值** | 30% | 锦上添花 | 提升效率 20-50% | 核心痛点，效率提升 50%+ |
| **数据就绪度** | 25% | 无电子化数据 | 部分电子化 | 数据完整、格式规范 |
| **技术可行性** | 20% | 技术不成熟 | 有参考案例 | 成熟方案、可直接复用 |
| **实施成本** | 15% | > 20 万 | 5-20 万 | < 5 万 |
| **安全合规** | 10% | L3+ 数据，需物理隔离 | L2 数据，需脱敏 | L1 公开数据 |

> **优先级 = 业务价值×0.3 + 数据就绪度×0.25 + 技术可行性×0.2 + 实施成本×0.15 + 安全合规×0.1**
>
> 得分 ≥ 4.0 → P0 立即启动；3.0-3.9 → P1 近期启动；2.0-2.9 → P2 排期等待；< 2.0 → P3 暂缓

##### 4.7.2 锅炉制造企业典型 AI 需求全景图

基于行业经验，预判各部门可能的 AI 需求（需通过访谈验证）：

| 部门 | 可能的 AI 需求 | 类型 | 预估优先级 |
|------|--------------|------|----------|
| **体系管理部** | 体系手册文本纠错 | A 类 | ★★★★★ P0（已规划） |
| **体系管理部** | 法规标准合规查询 | A 类 | ★★★★★ P0（已规划） |
| **生产部/质检部** | 焊接质量视觉检测 | C 类 | ★★★★☆ P1（已规划） |
| **生产部** | 焊接参数智能推荐 | D 类 | ★★★★☆ P1（已规划） |
| **质检部** | 表面缺陷检测（非焊接） | C 类 | ★★★☆☆ P2 |
| **质检部** | 检验报告自动生成 | A/B 类 | ★★★☆☆ P2 |
| **安全部** | 安全隐患图片识别 | C 类 | ★★★☆☆ P2 |
| **安全部** | 安全规程合规检查 | A 类 | ★★★☆☆ P2 |
| **采购部** | 供应商资质自动审查 | A 类 | ★★☆☆☆ P3 |
| **采购部** | 报价单智能比价 | E 类 | ★★☆☆☆ P3 |
| **财务部** | 合同条款风险审查 | A 类 | ★★★☆☆ P2 |
| **财务部** | 发票/报表数据提取 | E 类 | ★★☆☆☆ P3 |
| **人事部** | 培训材料智能生成 | A 类 | ★★☆☆☆ P3 |
| **人事部** | 岗位说明书自动生成 | A 类 | ★★☆☆☆ P3 |
| **技术部** | 工艺文件合规审查 | A 类 | ★★★☆☆ P2 |
| **技术部** | 设计规范智能检索 | A 类 | ★★★☆☆ P2 |
| **设备部** | 设备故障预测维护 | D 类 | ★★★☆☆ P2 |
| **设备部** | 备件消耗预测 | D 类 | ★★☆☆☆ P3 |

> **关键发现**：大部分需求（约 60%）属于 **A 类（LLM+RAG）**，万悟平台可直接支持，无需额外子系统。C 类和 D 类需求需要专业子系统，但训练均为一次性或周期性批处理，不占用日常推理资源。

#### 4.8 通用 AI 子系统接入模板

为支持未来未知需求的快速接入，设计通用子系统模板：

```
┌─────────────────────────────────────────────────────┐
│              通用 AI 子系统模板                        │
│                                                       │
│  ┌─────────────┐    ┌─────────────┐    ┌──────────┐ │
│  │ 数据管道     │    │ 模型服务     │    │ MCP 对接  │ │
│  │             │    │             │    │          │ │
│  │ · 数据采集   │ ─→ │ · 训练流水线 │ ─→ │ · 工具注册│ │
│  │ · 数据清洗   │    │ · 推理服务   │    │ · API 暴露│ │
│  │ · 特征工程   │    │ · 模型版本   │    │ · 结果格式│ │
│  └─────────────┘    └─────────────┘    └──────────┘ │
│                                                       │
│  标准化接口：                                          │
│  · 健康检查：GET /health                              │
│  · 推理调用：POST /predict                            │
│  · 训练触发：POST /train                              │
│  · 模型版本：GET /models                              │
│  · MCP 工具定义：标准 MCP tool schema                  │
└─────────────────────────────────────────────────────┘
```

**新需求接入流程**：

1. **需求访谈**（1 天）→ 使用 4.7.1 模板收集需求
2. **需求分类**（0.5 天）→ 判定 A/B/C/D/E 类型
3. **可行性评估**（1-2 天）→ 数据就绪度 + 技术可行性 + 成本估算
4. **方案设计**（2-3 天）→ A/B 类直接在万悟配置；C/D/E 类设计子系统
5. **开发实施**（2-8 周）→ 按类型使用对应模板
6. **MCP 集成**（1 周）→ 注册为万悟平台工具
7. **验收上线**（1 周）→ 效果验证 + 用户培训

#### 4.9 扩展场景实施路线图（修订版）

```
第一阶段（第 1-6 周）         第二阶段（第 7-16 周）        第三阶段（第 17-28 周）
┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│ P0：文本纠错落地   │     │ P1：专业子系统落地  │     │ P2：全公司推广     │
│                    │     │                    │     │                    │
│ · 万悟平台部署     │     │ · 焊接质检子系统   │     │ · 全部门需求收集   │
│ · 知识库构建       │ ──→ │ · 参数优化子系统   │ ──→ │ · A 类需求快速配置 │
│ · 纠错 Agent      │     │ · 训练流水线搭建   │     │ · C/D 类需求排期   │
│ · 安全护栏         │     │ · MCP 工具集成     │     │ · 通用模板复用     │
│ · 需求收集启动     │     │ · 夜间训练调度     │     │ · 持续优化迭代     │
│                    │     │                    │     │                    │
│ 投入：4-5.5 万     │     │ 投入：1.5-5 万     │     │ 投入：视需求而定   │
│ 累计：4-5.5 万     │     │ 累计：5.5-10.5 万  │     │ 累计：视需求而定   │
└──────────────────┘     └──────────────────┘     └──────────────────┘
        │                        │                        │
        │  同步进行               │  同步进行               │
        ▼                        ▼                        ▼
  部门访谈+需求收集         A 类需求快速配置          新子系统按需开发
  （体系/生产/质检）       （安全规程/合同审查）     （设备预测/能耗优化）
```

> **关键原则**：
> 1. **需求收集贯穿始终**：从第一天就开始全部门访谈，不等到技术平台建好再收集
> 2. **A 类需求零成本扩展**：万悟平台原生支持，只需配置知识库和 Agent，无需额外硬件
> 3. **训练不占推理资源**：所有训练任务安排在夜间/周末，日间推理不受影响
> 4. **通用模板加速交付**：C/D/E 类需求使用通用子系统模板，标准化接入流程

---

### 五、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| 本地模型纠错能力不足 | 检出率低 | 先用 API 模型（GLM-5.1/Qwen3.6-Max）验证效果上限，再选择合适的本地模型 |
| 法规标准文档不全 | 遗漏引用错误 | 建立标准文档持续收集机制 |
| AI 幻觉导致误报 | 人工复核成本高 | 纠错结果标注置信度，低置信度标注"建议人工复核" |
| 标准更新不及时 | 时效性检查失效 | 建立定期更新机制 + 联网检索辅助验证 |
| GPU 资源不足 | 推理速度慢 | 采用量化模型（Q4_K_M）或使用 API 模型分流 |

---

### 六、成本估算

#### 6.1 私有化部署方案（RTX 4090D）

| 项目 | 费用 | 说明 |
|------|------|------|
| GPU 显卡 | 1.3-1.8 万/张 | RTX 4090D 24GB GDDR6X，14592 CUDA 核心 |
| GPU 服务器整机 | 3-5 万 | 1×RTX 4090D + 64GB RAM + 1TB SSD（验证阶段） |
| 生产级服务器 | 8-12 万 | 2×RTX 4090D + 128GB RAM + 2TB SSD（生产阶段） |
| 万悟平台 | 0 元 | Apache 2.0 开源免费 |
| 开源模型 | 0 元 | Qwen3.6-27B (Apache 2.0) / GLM-5-Turbo (MIT) 免费商用 |
| 实施人力 | 内部调配 | 1 名 AI 工程师 + 1 名体系管理员 |
| 年度电费 | 约 0.8-1.5 万 | RTX 4090D TDP 425W，按 8h/天 × 250 天估算 |
| 年度运维 | 约 1-2 万 | 硬件维护 + 软件更新 |

**私有化部署一次性投入**：验证阶段约 **3-5 万**，生产阶段约 **8-12 万**

#### 6.2 API 调用方案

| 模型 | 输入价格（元/百万 Token） | 输出价格（元/百万 Token） | 平台 | 合规风险 |
|------|------------------------|------------------------|------|---------|
| **GLM-5.1** | 6 | 24 | 阿里云百炼（5 折节省计划后：输入 3 / 输出 12） | ✅ 低风险 |
| **Qwen3.6-Max** | 9 | 54 | 阿里云百炼 | ✅ 低风险 |
| **Qwen3-Max** | 2.5 | 10 | 阿里云百炼 | ✅ 低风险 |
| **GLM-4-Plus** | 5 | 5 | 智谱官方 | ✅ 低风险 |
| **GLM-4-Flash** | 免费 | 免费 | 智谱官方（轻量任务） | ✅ 低风险 |
| **DeepSeek-V4** | 1 | 4 | DeepSeek 官方 | ⛔ 高风险，不推荐敏感场景 |
| **DeepSeek-R1** | 4 | 16 | DeepSeek 官方 | ⛔ 高风险，不推荐敏感场景 |

> ⚠️ **DeepSeek API 价格虽低，但存在数据留存和训练使用风险**（详见 2.4.4 节）。仅适用于 L1 公开数据场景，L2+ 数据严禁使用。

**API 月度成本估算**（按输入:输出 ≈ 2:1 比例）：

| 使用强度 | 月 Token 量 | GLM-5.1（原价） | GLM-5.1（5 折后） | Qwen3-Max |
|---------|------------|---------------|-----------------|-----------|
| 轻度（个人偶尔用） | 50 万 | 约 ¥30 | 约 ¥15 | 约 ¥10 |
| 中度（日常办公辅助） | 500 万 | 约 ¥300 | 约 ¥150 | 约 ¥100 |
| 重度（团队+批量纠错） | 5000 万 | 约 ¥3,000 | 约 ¥1,500 | 约 ¥1,000 |
| 极重度（全公司高频） | 5 亿 | 约 ¥30,000 | 约 ¥15,000 | 约 ¥10,000 |

#### 6.3 私有化 vs API 成本对比

| 对比维度 | 私有化部署（RTX 4090D） | API 调用 |
|---------|----------------------|---------|
| **初始投入** | 3-5 万（验证）/ 8-12 万（生产） | 0 元 |
| **月度成本** | 电费约 ¥700-1,300 | 轻度 ¥15-30 / 中度 ¥150-300 / 重度 ¥1,500-3,000 |
| **年度总成本** | 首年 4-7 万（含硬件），后续 1.5-3 万/年 | 轻度 ¥200-400/年 / 中度 ¥2,000-4,000/年 / 重度 ¥2-4 万/年 |
| **回本周期** | — | 中度使用约 2-3 年回本，重度使用约 1 年回本 |
| **数据安全** | ★★★★★ 数据完全不出内网 | ★★★☆☆ 需上传至云端 |
| **模型能力** | Qwen3.6-27B 级别（强但非最强） | 可随时调用 GLM-5.1/Qwen3.6-Max 等最强模型 |
| **推理速度** | 10-20 t/s（受本地算力限制） | 20-40 t/s（云端集群） |
| **扩展性** | 需采购新硬件 | 弹性扩展，按需使用 |

#### 6.4 推荐方案：混合模式（数据分级路由）

针对锅炉制造企业的实际需求，推荐**私有化部署 + API 补充**的混合模式，核心原则是**按数据级别路由**：

**数据分级路由规则**

```
┌─────────────────────────────────────────────────────────────┐
│                     数据分级路由引擎                           │
│                                                               │
│   用户请求 ──→ 数据级别识别 ──→ 路由决策                       │
│                     │                                         │
│          ┌──────────┼──────────┐                              │
│          ▼          ▼          ▼                              │
│     L1 公开     L2 内部    L3+ 敏感                           │
│          │          │          │                              │
│          ▼          ▼          ▼                              │
│   ┌──────────┐ ┌──────────┐ ┌──────────┐                    │
│   │ API 调用  │ │ 脱敏+API │ │ 本地模型  │                    │
│   │ 或本地模型│ │ 或本地模型│ │ (强制)   │                    │
│   └──────────┘ └──────────┘ └──────────┘                    │
│                                                               │
│   L1：公开法规/标准/行业规范 → 可选 API（含 DeepSeek）         │
│   L2：内部制度/流程/管理规范 → 脱敏后 API 或本地模型           │
│   L3：财务/人事/经营数据     → 仅本地模型，数据不出内网         │
│   L3+：核心工艺/商业机密     → 仅本地模型，物理隔离环境         │
└─────────────────────────────────────────────────────────────┘
```

| 数据级别 | 定义 | 路由策略 | 允许的 API 供应商 | 示例 |
|---------|------|---------|----------------|------|
| **L1（公开）** | 已公开的法规、标准、行业规范 | 可选 API 或本地模型 | 阿里云/华为云/智谱/DeepSeek | GB/T 标准、行业法规 |
| **L2（内部）** | 企业内部制度、流程、管理规范 | 脱敏后 API 或本地模型 | 阿里云/华为云/智谱 | 内部管理制度、操作规程 |
| **L3（敏感）** | 财务、人事、经营数据 | **仅本地模型** | ❌ 禁止任何 API | 财务报表、薪酬数据 |
| **L3+（机密）** | 核心工艺、商业机密 | **仅本地模型+物理隔离** | ❌ 禁止任何 API | 核心焊接工艺、客户清单 |

**具体实施方案**

1. **本地部署 Qwen3.6-27B**：处理 L3 及以上敏感数据（财务/人事/核心工艺），数据不出内网
2. **API 调用 GLM-5.1 / Qwen3-Max**：处理 L1-L2 非敏感数据（法规查询/标准验证/公开资料分析），享受最强模型能力
3. **DeepSeek API**：仅用于 L1 公开数据场景（如查询公开法规标准），且必须经过数据脱敏处理
4. **成本控制策略**：
   - 优先使用本地模型，仅复杂/非敏感任务走 API
   - 购买阿里云百炼 5 折通用节省计划，降低 API 成本
   - 利用 GLM-4-Flash 免费额度处理轻量任务
   - DeepSeek API 仅在 L1 场景且其他 API 不可用时作为备选

**混合模式年度成本估算**：

| 项目 | 费用 | 说明 |
|------|------|------|
| 硬件（一次性） | 3-5 万 | 1×RTX 4090D 服务器 |
| 本地推理电费 | 约 0.8-1.5 万/年 | 日常敏感数据处理 |
| API 调用费 | 约 0.3-1 万/年 | 非敏感场景 + 复杂任务 |
| 运维 | 约 1 万/年 | 硬件维护 + 软件更新 |
| **首年总计** | **约 5-8 万** | 含硬件 |
| **后续年度** | **约 2-3.5 万/年** | 不含硬件 |

**对比 SaaS 方案**：同等功能的 SaaS AI 平台年费通常在 20-50 万，且数据安全风险高。混合模式首年成本仅为 SaaS 的 1/3-1/6，后续年度仅为 1/6-1/15。
